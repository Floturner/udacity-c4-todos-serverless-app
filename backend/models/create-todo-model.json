{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Create Todo",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "minLength": 3
        },
        "dueDate": {
            "type": "string",
            "format": "date"
        }
    },
    "required": ["name", "dueDate"],
    "additionalProperties": false
}
